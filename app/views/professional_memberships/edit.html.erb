<div class="container">
  <div class="row">
    <div class="form-group col-md-8 col-md-offset-2">
      <legend><h1>Editing Professional Membership for: <%= @person.title + ' ' +@person.full_name %></h1></legend>
      
      <%= render 'form', professional_membership: @professional_membership %>
      
      <%= link_to 'Back', person_path(@person) %>
    </div>
  </div>
<div>