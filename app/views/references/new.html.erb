<div class="container">
    <h1>New Reference for: <%= @person.title + ' ' +@person.full_name %></h1>

    <%= render 'form', reference: @reference %>

    <%= link_to 'Back', person_path(@person) %>
</div>