<div class="container">
  <div class="row">
    <div class="form-group col-md-8 col-md-offset-2">
      <legend><h1>Editing Employment History for: <%= @person.title + ' ' +@person.full_name %></h1></legend>
  
      <%= render 'form', employment_history: @employment_history %>
  
      <%= link_to 'Back', person_path(@person), class: 'btn btn-primary' %>
    </div>
  </div>
</div>