<div class="container">
    <h1>Editing Qualification for: <%= @person.title + ' ' +@person.full_name %></h1>
    
    <%= render 'form', qualification: @qualification %>
    
    <%= link_to 'Back', person_path(@person) %>
</div>