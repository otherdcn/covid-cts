<%= form_with(model: skill_type, local: true) do |form| %>
  <% if skill_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skill_type.errors.count, "error") %> prohibited this skill_type from being saved:</h2>

      <ul>
      <% skill_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>Required fields are followed by <abbr title="Required">*</abbr></p>

  <div class="form-row">
    <div class="form-group col-md-12">
      <%= form.label :type_name %> <abbr title="Required" aria-label="required">*</abbr> <br />
      <%= form.text_field :type_name, class: "form-control" %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %> <hr />
  </div>
<% end %>
